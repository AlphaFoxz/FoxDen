# FoxDen BOM

Bill of Materials module for centralized dependency management.

This module uses the `java-platform` Gradle plugin to define dependency versions
that are shared across all FoxDen modules.
